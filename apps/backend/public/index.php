<?php
// Front controller for Flight PHP application
// This will bootstrap the app and let Flight handle routes defined in app/config/routes.php
$ds = DIRECTORY_SEPARATOR;
require __DIR__ . $ds . '..' . $ds . 'app' . $ds . 'config' . $ds . 'bootstrap.php';

